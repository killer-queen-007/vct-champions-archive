const vctData = {
  2023: {
    location: "洛杉矶",
    champion: "Evil Geniuses",
    shortChampion: "EG",
    mvp: "Demon1",
    date: "2023.08.06 - 08.26",
    result: "总决赛：EG 3 - 1 PRX",
    theme: "red",
    moments: [
      "EG 从败者组一穿五夺冠",
      "Demon1 的大狙统治了洛杉矶",
      "这是北美 Valorant 的黎明"
    ],
    echoes: [
      "他们拒绝坠落，他们把命运按在地上。",
      "每一发子弹都在回答质疑。",
      "冠军不是童话，是血与火之后的沉默。"
    ],
    players: [
      { rank: 1, name: "Demon1", country: "美国", team: "EG", agent: "捷风", acs: 280, kd: 1.45, adr: 185, kast: "78%", kpr: 0.92, dpr: 0.63, fb: 42, fd: 18, fbsr: 2.33, hs: "32%", clutch: 84, quote: "我就是为此而生的" },
      { rank: 2, name: "Ethan", country: "美国", team: "EG", agent: "斯凯", acs: 236, kd: 1.26, adr: 151, kast: "79%", kpr: 0.81, dpr: 0.65, fb: 24, fd: 19, fbsr: 1.26, hs: "24%", clutch: 80, quote: "不退，就是胜利的开始" },
      { rank: 3, name: "jawgemo", country: "美国", team: "EG", agent: "雷兹", acs: 244, kd: 1.19, adr: 160, kast: "74%", kpr: 0.84, dpr: 0.71, fb: 36, fd: 28, fbsr: 1.29, hs: "21%", clutch: 74, quote: "我要先开第一枪" },
      { rank: 4, name: "C0M", country: "美国", team: "EG", agent: "猎枭", acs: 214, kd: 1.11, adr: 136, kast: "77%", kpr: 0.72, dpr: 0.65, fb: 18, fd: 17, fbsr: 1.06, hs: "27%", clutch: 70, quote: "信息就是刀锋" },
      { rank: 5, name: "Boostio", country: "美国", team: "EG", agent: "奇乐", acs: 205, kd: 1.02, adr: 130, kast: "75%", kpr: 0.69, dpr: 0.67, fb: 15, fd: 22, fbsr: 0.68, hs: "20%", clutch: 68, quote: "我会带他们走到最后" },
      { rank: 6, name: "something", country: "俄罗斯", team: "PRX", agent: "捷风", acs: 274, kd: 1.31, adr: 176, kast: "73%", kpr: 0.89, dpr: 0.68, fb: 39, fd: 30, fbsr: 1.3, hs: "28%", clutch: 72, quote: "下一发，就是答案" }
    ]
  },
  2024: {
    location: "首尔",
    champion: "EDward Gaming",
    shortChampion: "EDG",
    mvp: "ZmjjKK",
    date: "2024.08.01 - 08.25",
    result: "总决赛：EDG 3 - 2 TH",
    theme: "cyan",
    moments: [
      "EDG 五局鏖战击溃 TH",
      "ZmjjKK 关键局狙击改写走向",
      "中国赛区登顶世界之巅"
    ],
    echoes: [
      "他们做到了！EDG 创造了历史！",
      "首尔的灯光照见了新的王朝。",
      "这一冠，替无数个不眠夜作证。"
    ],
    players: [
      { rank: 1, name: "ZmjjKK", country: "中国", team: "EDG", agent: "捷风", acs: 287, kd: 1.38, adr: 182, kast: "77%", kpr: 0.94, dpr: 0.68, fb: 44, fd: 23, fbsr: 1.91, hs: "31%", clutch: 86, quote: "准星会替我说话" },
      { rank: 2, name: "CHICHOO", country: "中国", team: "EDG", agent: "奇乐", acs: 231, kd: 1.24, adr: 149, kast: "80%", kpr: 0.78, dpr: 0.63, fb: 18, fd: 16, fbsr: 1.13, hs: "26%", clutch: 83, quote: "稳住，就是赢下残局" },
      { rank: 3, name: "Smoggy", country: "中国", team: "EDG", agent: "雷兹", acs: 248, kd: 1.21, adr: 159, kast: "76%", kpr: 0.82, dpr: 0.68, fb: 34, fd: 25, fbsr: 1.36, hs: "23%", clutch: 76, quote: "给我空间，我会终结" },
      { rank: 4, name: "nobody", country: "中国", team: "EDG", agent: "猎枭", acs: 219, kd: 1.14, adr: 142, kast: "79%", kpr: 0.75, dpr: 0.66, fb: 21, fd: 18, fbsr: 1.17, hs: "25%", clutch: 78, quote: "情报落地，节奏就归我们" },
      { rank: 5, name: "S1Mon", country: "中国", team: "EDG", agent: "欧门", acs: 207, kd: 1.03, adr: 132, kast: "75%", kpr: 0.69, dpr: 0.67, fb: 12, fd: 17, fbsr: 0.71, hs: "20%", clutch: 70, quote: "烟雾后面，是计划本身" },
      { rank: 6, name: "Meteor", country: "韩国", team: "GEN", agent: "雷兹", acs: 263, kd: 1.29, adr: 170, kast: "74%", kpr: 0.87, dpr: 0.67, fb: 37, fd: 29, fbsr: 1.28, hs: "29%", clutch: 74, quote: "舞台越大，越要向前" }
    ]
  },
  2025: {
    location: "巴黎",
    champion: "Sentinels",
    shortChampion: "SEN",
    mvp: "TenZ",
    date: "2025.08.05 - 08.30",
    result: "总决赛：SEN 3 - 2 FNC",
    theme: "gold",
    moments: [
      "巴黎雨夜，SEN 完成逆转",
      "TenZ 在决胜图打出传奇残局",
      "金色雨落下时，全场沸腾"
    ],
    echoes: [
      "这不是回归，这是王者宣言。",
      "有人在巴黎封神，也有人在巴黎告别。",
      "冠军奖杯从不记得亚军的眼泪。"
    ],
    players: [
      { rank: 1, name: "TenZ", country: "加拿大", team: "SEN", agent: "捷风", acs: 291, kd: 1.41, adr: 188, kast: "79%", kpr: 0.96, dpr: 0.68, fb: 47, fd: 24, fbsr: 1.96, hs: "34%", clutch: 88, quote: "最后一图，我只看准星" },
      { rank: 2, name: "zekken", country: "美国", team: "SEN", agent: "雷兹", acs: 276, kd: 1.32, adr: 177, kast: "76%", kpr: 0.91, dpr: 0.69, fb: 41, fd: 29, fbsr: 1.41, hs: "27%", clutch: 79, quote: "我会一直冲到终点" },
      { rank: 3, name: "johnqt", country: "摩洛哥", team: "SEN", agent: "幽影", acs: 224, kd: 1.18, adr: 145, kast: "80%", kpr: 0.76, dpr: 0.64, fb: 19, fd: 17, fbsr: 1.12, hs: "25%", clutch: 81, quote: "指挥不是声音，是方向" },
      { rank: 4, name: "Sacy", country: "巴西", team: "SEN", agent: "斯凯", acs: 216, kd: 1.11, adr: 139, kast: "81%", kpr: 0.73, dpr: 0.66, fb: 15, fd: 14, fbsr: 1.07, hs: "24%", clutch: 80, quote: "经验就是关键回合的呼吸" },
      { rank: 5, name: "Zellsis", country: "美国", team: "SEN", agent: "蝰蛇", acs: 211, kd: 1.05, adr: 134, kast: "77%", kpr: 0.7, dpr: 0.67, fb: 14, fd: 19, fbsr: 0.74, hs: "22%", clutch: 73, quote: "我在脏活里拿胜利" },
      { rank: 6, name: "Derke", country: "芬兰", team: "FNC", agent: "捷风", acs: 279, kd: 1.34, adr: 181, kast: "75%", kpr: 0.9, dpr: 0.67, fb: 43, fd: 32, fbsr: 1.34, hs: "31%", clutch: 76, quote: "我想把奖杯留在欧洲" }
    ]
  }
};

let currentYear = "2023";
let currentPage = 1;
const pageSize = 5;
let searchKeyword = "";
let sortField = "acs";

const medalMap = { 1: "🥇", 2: "🥈", 3: "🥉" };

const transitionTargets = document.querySelectorAll(".transition-target");
const yearButtons = document.querySelectorAll(".year-btn, .year-stop");
const siteNav = document.getElementById("siteNav");
const playerGrid = document.getElementById("playerGrid");
const playerTemplate = document.getElementById("playerCardTemplate");
const championName = document.getElementById("championName");
const championMvp = document.getElementById("championMvp");
const championResult = document.getElementById("championResult");
const championDate = document.getElementById("championDate");
const championLogo = document.getElementById("championLogo");
const momentDots = document.getElementById("momentDots");
const momentQuote = document.getElementById("momentQuote");
const tableBody = document.getElementById("tableBody");
const pagination = document.getElementById("pagination");
const echoQuotes = document.getElementById("echoQuotes");
const searchInput = document.getElementById("searchInput");
const sortSelect = document.getElementById("sortSelect");
const menuToggle = document.getElementById("menuToggle");
const mobileMenu = document.getElementById("mobileMenu");

function parseKast(value) {
  return Number(String(value).replace("%", "")) || 0;
}

function getPlayersForTable() {
  const players = vctData[currentYear].players.map((player) => ({
    ...player,
    kastValue: parseKast(player.kast)
  }));
  const filtered = players.filter((player) => {
    const key = searchKeyword.toLowerCase();
    return player.name.toLowerCase().includes(key) || player.team.toLowerCase().includes(key);
  });
  filtered.sort((a, b) => b[sortField] - a[sortField]);
  return filtered;
}

function syncYearButtons() {
  yearButtons.forEach((button) => {
    button.classList.toggle("active", button.dataset.year === currentYear);
  });
}

function renderMoments(data) {
  momentDots.innerHTML = "";
  data.moments.forEach((item, index) => {
    const dot = document.createElement("button");
    dot.type = "button";
    dot.className = "moment-dot";
    if (index === 0) dot.classList.add("active");
    dot.addEventListener("click", () => {
      momentQuote.textContent = `“${item}”`;
      momentDots.querySelectorAll(".moment-dot").forEach((d, i) => {
        d.classList.toggle("active", i === index);
      });
    });
    momentDots.appendChild(dot);
  });
  momentQuote.textContent = `“${data.moments[0]}”`;
}

function renderChampion() {
  const data = vctData[currentYear];
  championName.textContent = data.champion;
  championMvp.textContent = data.mvp;
  championResult.textContent = data.result;
  championDate.textContent = data.date;
  championLogo.textContent = data.shortChampion;
  renderMoments(data);
}

function renderPlayers() {
  const data = vctData[currentYear];
  playerGrid.innerHTML = "";
  data.players.forEach((player) => {
    const fragment = playerTemplate.content.cloneNode(true);
    const card = fragment.querySelector(".player-card");
    fragment.querySelector(".medal").textContent = medalMap[player.rank] || "";
    fragment.querySelector(".player-name").textContent = player.name;
    fragment.querySelector(".player-meta").textContent = `${player.country} | ${player.team}`;
    fragment.querySelector(".player-agent").textContent = `招牌特工：${player.agent}`;
    fragment.querySelector(".detail-stats").textContent =
      `ACS ${player.acs} | K/D ${player.kd} | ADR ${player.adr} | KAST ${player.kast} | FB ${player.fb}`;
    fragment.querySelector(".player-quote").textContent = `“${player.quote}”`;
    card.addEventListener("click", () => {
      card.classList.toggle("expanded");
    });
    playerGrid.appendChild(fragment);
  });
}

function createRankBadge(rank) {
  if (rank === 1) return '<span class="rank-top1">1</span>';
  if (rank === 2) return '<span class="rank-top2">2</span>';
  if (rank === 3) return '<span class="rank-top3">3</span>';
  return String(rank);
}

function renderTable() {
  const rows = getPlayersForTable();
  const totalPages = Math.max(1, Math.ceil(rows.length / pageSize));
  currentPage = Math.min(currentPage, totalPages);
  const start = (currentPage - 1) * pageSize;
  const pageRows = rows.slice(start, start + pageSize);

  tableBody.innerHTML = pageRows.map((player) => `
    <tr>
      <td>${createRankBadge(player.rank)}</td>
      <td>${player.name}</td>
      <td>${player.team}</td>
      <td>${player.agent}</td>
      <td>${player.acs}</td>
      <td>${player.kd}</td>
      <td>${player.adr}</td>
      <td>${player.kast}</td>
      <td>${player.kpr}</td>
      <td>${player.dpr}</td>
      <td class="fb-cell">${player.fb}</td>
      <td class="fd-cell">${player.fd}</td>
      <td>${player.fbsr}</td>
      <td>${player.hs}</td>
    </tr>
  `).join("");

  pagination.innerHTML = "";
  for (let page = 1; page <= totalPages; page += 1) {
    const button = document.createElement("button");
    button.type = "button";
    button.className = "page-btn";
    button.textContent = String(page);
    if (page === currentPage) button.classList.add("active");
    button.addEventListener("click", () => {
      currentPage = page;
      renderTable();
    });
    pagination.appendChild(button);
  }
}

function renderEcho() {
  const data = vctData[currentYear];
  echoQuotes.innerHTML = data.echoes.map((quote) => `<p class="echo-item">"${quote}"</p>`).join("");
}

function fadeAndRefresh() {
  transitionTargets.forEach((el) => el.classList.add("fading"));
  window.setTimeout(() => {
    document.body.dataset.theme = vctData[currentYear].theme;
    syncYearButtons();
    renderChampion();
    renderPlayers();
    renderTable();
    renderEcho();
    transitionTargets.forEach((el) => el.classList.remove("fading"));
  }, 180);
}

function setupYearSwitch() {
  yearButtons.forEach((button) => {
    button.addEventListener("click", () => {
      const year = button.dataset.year;
      if (!year || year === currentYear) return;
      currentYear = year;
      currentPage = 1;
      fadeAndRefresh();
    });
  });
}

function setupRevealAnimation() {
  const sections = document.querySelectorAll(".reveal-section");
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("visible");
      }
    });
  }, { threshold: 0.12 });

  sections.forEach((section, index) => {
    section.style.transitionDelay = `${index * 0.1}s`;
    observer.observe(section);
  });
}

function setupNavBehavior() {
  window.addEventListener("scroll", () => {
    siteNav.classList.toggle("scrolled", window.scrollY > 100);
  });
}

function setupControls() {
  searchInput.addEventListener("input", (event) => {
    searchKeyword = event.target.value.trim();
    currentPage = 1;
    renderTable();
  });

  sortSelect.addEventListener("change", (event) => {
    sortField = event.target.value;
    currentPage = 1;
    renderTable();
  });
}

function setupMobileMenu() {
  menuToggle.addEventListener("click", () => {
    const open = mobileMenu.classList.toggle("open");
    menuToggle.setAttribute("aria-expanded", String(open));
  });

  mobileMenu.querySelectorAll("a").forEach((link) => {
    link.addEventListener("click", () => {
      mobileMenu.classList.remove("open");
      menuToggle.setAttribute("aria-expanded", "false");
    });
  });
}

function init() {
  setupYearSwitch();
  setupControls();
  setupRevealAnimation();
  setupNavBehavior();
  setupMobileMenu();
  fadeAndRefresh();
}

init();
